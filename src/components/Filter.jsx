import React from 'react';

const Filter = ()=>{
    return (
        <div className="filterContainer">
            <button className="AllButton">All</button>
            <button className="comedyButton">Comedy</button>
            <button className="ActionButton">Action</button>

        </div>
    );
};

export default Filter;